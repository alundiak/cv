// module.exports = function savePdfFile(require) {
    const React = require('react');
    const ReactPDF = require('@react-pdf/renderer');
    // const Quixote = require('./components/v2/Quixote');

    // ReactPDF.render(<Quixote />, `${__dirname}/node_rendered.pdf`);
// };

console.log(ReactPDF.render);

// exports.printMsg = function() {
//     console.log("This is a message from the demo package");
// }
// printMsg();